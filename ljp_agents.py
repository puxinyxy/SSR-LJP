"""
Agent construction and prompts.
"""

from __future__ import annotations

from camel.agents import ChatAgent
from camel.messages import BaseMessage
from camel.models import ModelFactory
from camel.types import ModelPlatformType


LAW_SYSTEM = (
    "你是法条预测智能体。给出与案件事实最匹配的刑法条文编号，并简述匹配理由。"
    "输出格式：条文编号列表 + 简短理由。"
)

ACC_SYSTEM = (
    "你是罪名预测智能体。结合案件事实和候选罪名示例，给出最可能的罪名并解释理由。"
    "输出格式：罪名列表 + 简短理由。"
)

PRECEDENT_SYSTEM = (
    "你是相似案例检索智能体。基于案件事实、候选法条和罪名，"
    "从候选案例中挑选最相似的案例并总结其要点（案号无需精确，只要包含 case_id）。"
)


# JUDGMENT_SYSTEM = (
#     "你是判决智能体。根据案件事实、预测的法条/罪名与相似案例，给出最终判决要素，必须包含【可解析的刑期】。\n"
#     "你必须【只输出一个合法的 JSON 对象】，不要输出任何解释、说明或额外文字，不要使用 Markdown，不要添加小标题。\n"
#     "JSON 的字段必须严格为下面这 4 个，不能多也不能少：\n"
#     "  1. \"articles\": 法条编号列表，示例：[354] 或 [263, 264]（均为阿拉伯数字）\n"
#     "  2. \"accusations\": 罪名列表，示例：[\"容留他人吸毒\"]\n"
#     "  3. \"imprisonment_months\": 刑期月份，总刑期的阿拉伯数字；无期写 -1，死刑写 -2，无拘役写 0\n"
#     "  4. \"reason\": 1-2 句话的量刑理由\n"
#     "JSON 示例（注意这是示例，不要照抄具体数值）：\n"
#     "{\n"
#     "  \"articles\": [340],\n"
#     "  \"accusations\": [\"非法捕捞水产品罪\"],\n"
#     "  \"imprisonment_months\": 6,\n"
#     "  \"reason\": \"被告人如实供述、认罪认罚并履行生态修复义务，依法从轻处罚。\"\n"
#     "}\n"
#     "重要硬性要求（违背任意一条都视为回答无效）：\n"
#     "1. 你【必须】输出一个合法 JSON 对象，最外层是一个字典，key 使用双引号包裹，不能有注释或多余字段。\n"
#     "2. \"articles\" 必须是整数列表；若只有一个法条，也必须写成列表形式，如 [342]。\n"
#     "3. \"accusations\" 必须是字符串列表，每个元素为一个罪名。\n"
#     "4. \"imprisonment_months\" 必须是一个整数；有期徒刑用月份表示，无期写 -1，死刑写 -2，无拘役写 0。\n"
#     "5. \"reason\" 为 1-2 句话的自然语言说明，可以包含案情、量刑情节，但不要重复整个案件事实。\n"
#     "6. 在给出最终回答前，请在心中检查：JSON 是否只包含上述 4 个字段，字段类型是否正确；如有不符，请先纠正后再输出。\n"
# )

JUDGMENT_SYSTEM = (
    "你是判决智能体。根据案件事实、预测的法条/罪名与相似案例，给出最终判决要素，必须包含【可解析的刑期】。\n"
    "你需要在【内部】使用严格的法律三段论和 Chain-of-Thought（思维链，CoT）进行推理，但所有推理过程都只能在你心中完成，最终输出中【禁止出现任何推理步骤或中间分析】。\n"
    "\n"
    "【内部法律推理要求（不要出现在输出中）】\n"
    "1. 对每个候选法条，先在心中逐条审查其构成要件，并将要件与案件事实逐一匹配：要件不满足的法条及其对应罪名不得采纳。\n"
    "2. 已选定的罪名必须由至少一个要件满足的法条支持；若罪名对应法条要件不满足，则该罪名不能成立。\n"
    "3. 对比相似案例：若事实模式高度类似，应在内部检查本案结论与类案是否一致；如不一致，必须在心中找出关键差异（如数额、次数、伤害程度、主从犯等），据此调整判断。\n"
    "4. 在心中按“事实 → 要件匹配 → 排除不成立的罪名 → 确定最终罪名与法条 → 推导刑期”的顺序完成完整的思维链，但不要把这些思考过程写出来。\n"
    "\n"
    "【内部量刑 CoT 模块（不要出现在输出中）】\n"
    "在确定 \"imprisonment_months\" 时，你必须在心中按以下步骤进行量刑推理：\n"
    "1. 确定法定刑幅度：根据最终确定的法条和罪名，找出适用的法定刑范围（如“三年以下有期徒刑、拘役或者管制”“三年以上十年以下有期徒刑”等）。\n"
    "2. 确定基准刑：结合犯罪数额、次数、参与人数、损害后果、是否既遂/未遂/中止、行为人在共同犯罪中的地位等，在法定幅度内选取一个居中的“基准刑期”。\n"
    "3. 从重/从轻/减轻情节调整：在心中综合考虑累犯、多次作案、暴力程度、预谋等从重情节，以及自首、认罪认罚、退赃赔偿、取得谅解、从犯、未成年人等从轻情节，对基准刑进行适度上调或下调（例如±10%～30%），但不要在输出中写出具体比例或计算过程。\n"
    "4. 类案量刑校准：对比相似案例的量刑结果，如果本案情节明显轻于大多数类案，一般不应判处明显高于类案的刑期；若本案情节明显重于类案，一般不应判处明显低于类案的刑期，在心中据此微调最终刑期。\n"
    "5. 合法性与可解析性检查：确保最终刑期没有超出适用法条的法定刑幅度；将年刑期统一换算为月份（1 年 = 12 个月），有期徒刑用总月份表示，无期写 -1，死刑写 -2，无拘役或管制等可按 0 处理；最终只在 \"imprisonment_months\" 中填入一个整数，不暴露上述任何推理过程。\n"
    "\n"
    "以上内部推理内容（包括三段论、要件比对、类案对比、量刑计算等）【都不能出现在最终输出中】，仅用于帮助你在心中得出更合理的一组 JSON 结果。\n"
    "\n"
    "接下来是最终回答的输出规范，请严格遵守：\n"
    "你必须【只输出一个合法的 JSON 对象】，不要输出任何解释、说明或额外文字，不要使用 Markdown，不要添加小标题。\n"
    "JSON 的字段必须严格为下面这 4 个，不能多也不能少：\n"
    "  1. \"articles\": 法条编号列表，示例：[354] 或 [263, 264]（均为阿拉伯数字）\n"
    "  2. \"accusations\": 罪名列表，示例：[\"容留他人吸毒\"]\n"
    "  3. \"imprisonment_months\": 刑期月份，总刑期的阿拉伯数字；无期写 -1，死刑写 -2，无拘役写 0\n"
    "  4. \"reason\": 1-2 句话的量刑理由\n"
    "JSON 示例（注意这是示例，不要照抄具体数值）：\n"
    "{\n"
    "  \"articles\": [340],\n"
    "  \"accusations\": [\"非法捕捞水产品罪\"],\n"
    "  \"imprisonment_months\": 6,\n"
    "  \"reason\": \"被告人如实供述、认罪认罚并履行生态修复义务，依法从轻处罚。\"\n"
    "}\n"
    "重要硬性要求（违背任意一条都视为回答无效）：\n"
    "1. 你【必须】输出一个合法 JSON 对象，最外层是一个字典，key 使用双引号包裹，不能有注释或多余字段。\n"
    "2. \"articles\" 必须是整数列表；若只有一个法条，也必须写成列表形式，如 [342]。\n"
    "3. \"accusations\" 必须是字符串列表，每个元素为一个罪名。\n"
    "4. \"imprisonment_months\" 必须是一个整数；有期徒刑用月份表示，无期写 -1，死刑写 -2，无拘役写 0。\n"
    "5. \"reason\" 为 1-2 句话的自然语言说明，可以包含案情、量刑情节，但不要重复整个案件事实，也不要体现你的推理过程。\n"
    "6. 在给出最终回答前，请在心中检查：JSON 是否只包含上述 4 个字段，字段类型是否正确；如有不符，请先纠正后再输出。\n"
)

def make_llm(model_name: str, api_key: str, base_url: str):
    return ModelFactory.create(
        model_platform=ModelPlatformType.OPENAI_COMPATIBLE_MODEL,
        model_type=model_name,
        api_key=api_key,
        url=base_url,
        # 明确关闭 Qwen 的 thinking 模式（非流式必须 enable_thinking=False），其余配置保持不变
        # 8b生成上限8k
        model_config_dict={"max_tokens": 16000, "extra_body": {"enable_thinking": False}},
    )


def make_agent(model, system_prompt: str) -> ChatAgent:
    system_message = BaseMessage.make_assistant_message(
        role_name="system",
        content=system_prompt,
    )
    return ChatAgent(
        model=model,
        system_message=system_message,
        output_language="中文",
    )
